root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -buildvcs=false -o ./tmp/ingester ./cmd/ingester"
  bin = "./tmp/ingester"
  include_ext = ["go", "toml", "env"]
  exclude_dir = ["vendor", "tmp", "bin"]

[log]
  time = true