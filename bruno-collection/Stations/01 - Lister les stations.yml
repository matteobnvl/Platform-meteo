info:
  name: Lister les stations
  type: http
  seq: 1

http:
  method: GET
  url: "http://localhost:8080/stations"
  auth: inherit
  params:
    - name: country
      value: FR
      enabled: false
      type: query

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
