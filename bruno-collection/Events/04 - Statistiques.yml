info:
  name: Statistiques des evenements
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8080/events/stats
  params:
    - name: type
      value: heatwave
      type: query
    - name: country
      value: FR
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
