info:
  name: Evenements d'une station
  type: http
  seq: 3

http:
  method: GET
  url: "http://localhost:8080/stations/07149/events"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
